import ArticlesList from "../reusable/ArticlesList";

export function AllArticles (){
    return (
        <main>
            <p>Sort By</p>
            <select name="" id=""></select>
            <p>Order</p>
            <select name="" id=""></select>
            <ArticlesList/>
        </main>
    )
}